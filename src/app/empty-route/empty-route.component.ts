import { Component } from '@angular/core';

@Component({
  /* dependencia do single-spa (pode ser mantido este seletor sempre) */
  selector: 'clean-arch-angular-empty-route',
  template: '',
})
export class EmptyRouteComponent {
}
